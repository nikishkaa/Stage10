package by.itstep.goutor.javatask.stage10.task.task6.util;

public class ParityUI {
    public static String getParityUI(boolean parity) {

        String allParity = parity ? "parity" : "dont parity";

        return "All digit is " + allParity;
    }
}
